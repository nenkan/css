{
  "name": "@nenkan/css",
  "version": "0.8.0",
  "description": "A set of styles shared by Nenkan applications",
  "main": "index.scss",
  "scripts": {
    "build": "sass --style=compressed style.scss dist/style.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "nenkan",
  "license": "MIT",
  "source": true,
  "devDependencies": {
    "sass": "^1.51.0"
  }
}
